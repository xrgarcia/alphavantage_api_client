[pytest]
markers =
    integration: Integration tests with alphavantage web api
    unit: Unit tests that should run fast and often
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
log_level=INFO
